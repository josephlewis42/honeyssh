# Passwords file, lists passwords that will work for each username.
# Keys are the username and values are an array of strings.
# The special username "*" provides passwords that work for any username an
# attacker supplies.
"*":

root:
